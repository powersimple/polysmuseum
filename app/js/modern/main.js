// Modern JavaScript entry point
import MegaMenu from './megamenu.js';

// Initialize modern components when DOM is ready
document.addEventListener('DOMContentLoaded', () => {
    // Initialize modern megamenu
    const modernMenu = new MegaMenu();
    modernMenu.init();

    // Add any additional modern component initializations here
}); 